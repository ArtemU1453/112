# ЕАСУР — запрет по умолчанию (Network Architecture: default-deny + явные разрешения).
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: emergency-112
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: emergency-112-data
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
