# ЕАСУР — Production. ТОЛЬКО имена переменных; значения — из секрет-менеджера (Security Standards).
# Реальные секреты НИКОГДА не коммитятся (Repository/Environment Governance Standards).
SPRING_PROFILES_ACTIVE=k8s
DB_URL=
DB_USER=
DB_PASSWORD=
REDIS_HOST=
KAFKA_BOOTSTRAP_SERVERS=
KEYCLOAK_ISSUER_URI=
LOG_LEVEL=INFO
