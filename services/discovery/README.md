# discovery (плейсхолдер)

**Статус:** планируется (не реализовано). Каталог-плейсхолдер (Stage 2 структура monorepo).

- **Назначение:** обнаружение сервисов (service discovery).
- **Примечание:** на текущей Baseline маршрутизация — через API Gateway и DNS Kubernetes;
  введение отдельного discovery — только через ADR/RFC.
