# tests/ — кросс-сервисные тесты

Contract- и E2E-тесты, охватывающие несколько сервисов (Testing Strategy). Тесты отдельного
сервиса располагаются рядом с его кодом (`src/test`, `ai-service/tests`). Реальные ПДн в тестах
запрещены — только синтетические/обезличенные данные (EGS).
