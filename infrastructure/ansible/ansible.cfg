# ЕАСУР — конфигурация Ansible (ADR-010).
[defaults]
inventory = inventory/hosts.ini
host_key_checking = True
retry_files_enabled = False
stdout_callback = yaml
roles_path = roles
forks = 10

[privilege_escalation]
become = True
become_method = sudo
